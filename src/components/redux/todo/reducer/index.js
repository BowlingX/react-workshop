/**
 * @flow
 */

const todoReducer = (state : Array<Object> = [], action:Object) => {
  switch (action.type) {
    case 'ADD':
      break;
    default:
      return state;
  }
};

export default todoReducer;
